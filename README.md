# Clash Rule Provider 前端组件

Clash Rule Provider 的前端界面实现，基于 **Vue 3** + **Vite** + **Vuetify** 开发。
作为一个 **Module Federation** 远程模块（Remote），集成到 MoviePilot 主应用中运行。

## ✨ 功能特性

- **规则管理**：增加、修改、删除 Clash 规则。
- **代理组配置**：基于 Meta 内核，支持灵活的路由配置。
- **状态监控**：查看流量统计和节点状态。
- **可视化配置**：提供友好的图形界面管理复杂的 YAML 配置。

## 🚀 开发指南

### 1. 环境准备

确保本地已安装 Node.js (推荐 v16+)。

### 2. 安装依赖

```bash
npm install
# or
yarn install
```

### 3. 启动开发服务器

```bash
npm run dev
# or
yarn dev
```

启动后，服务将运行在 `http://localhost:5001`。
> 注意：这是一个远程模块，单独访问可能无法完整运行所有功能，通常需要宿主应用（MoviePilot）来加载。

### 4. 构建生产版本

```bash
npm run build
# or
yarn build
```

构建产物将输出到 `dist` 目录。

## 📦 模块联邦 (Module Federation)

本项目配置为远程模块 (Remote)，向宿主应用暴露以下组件：

| 模块名称 | 对应组件 | 说明 |
| :--- | :--- | :--- |
| `./Page` | `src/components/Page.vue` | 插件主详情页面 |
| `./Config` | `src/components/Config.vue` | 插件配置页面 |
| `./Dashboard` | `src/components/Dashboard.vue` | 仪表盘概览组件 |

### 共享依赖 (Shared Dependencies)

为了减小体积并避免多实例冲突，以下依赖配置为 `shared`，由宿主应用提供：
- `vue`
- `vuetify`

> **注意**：在构建时，这些依赖不会被打包进 `remoteEntry.js`，确保宿主应用已正确提供。

## 📂 项目结构

```
plugin-component/
├── src/
│   ├── assets/        # 静态资源
│   ├── components/    # 组件源码
│   │   ├── cards/     # 信息卡片组件
│   │   ├── dialog/    # 弹窗组件
│   │   ├── tables/    # 表格组件
│   │   ├── tabs/      # 标签页组件
│   │   ├── Page.vue   # [Exposed] 主页面
│   │   ├── Config.vue # [Exposed] 配置页
│   │   └── ...
│   ├── styles/        # 全局样式
│   ├── vuetify/       # Vuetify 主题配置
│   ├── App.vue        # 开发环境入口
│   └── main.js        # 入口文件
├── vite.config.js     # Vite & 模块联邦配置
└── package.json       # 依赖配置
```